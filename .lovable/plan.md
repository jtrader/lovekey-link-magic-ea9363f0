## Change

In `src/routes/rsp.tsx` (hero subtext, line 928), update the sentence so it ends with the new phrase and links "Love Key Link" to the home page.

**Before:**
> RSP is a privacy-first coordination framework built into the core of Love Key Link.

**After:**
> RSP is a privacy-first coordination framework built into the core of [Love Key Link](/) our practical real life example application.

Where "Love Key Link" becomes a hyperlink to `/` (the home page), styled inline to match the surrounding text.

No other changes.